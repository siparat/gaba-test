model Coupon {
    id             String   @id @default(uuid())
    code           String   @unique
    discount       Int
    activatedCount Int      @default(0)
    maxActivations Int
    expiredAt      DateTime
    createdAt      DateTime @default(now())
}
